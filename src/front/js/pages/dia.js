import React, { useState, useEffect, useContext } from "react";
import { Context } from "../store/appContext";
import "../../styles/pages/dia.css";

export const Dia = () => {
    const { store, actions } = useContext(Context);
    
    return(
        <div></div>
    );
};